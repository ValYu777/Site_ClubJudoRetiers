{% extends 'base.html.twig' %}

{% block title %}Ajouter une photo{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1>Ajouter une nouvelle photo</h1>

        {{ form_start(form) }}
            {{ form_row(form.titre) }}
            {{ form_row(form.imageFile) }}
            <button class="btn btn-success mt-3">Enregistrer</button>
        {{ form_end(form) }}

        <a href="{{ path('galerie') }}" class="btn btn-secondary mt-3">Retour à la galerie</a>
    </div>
{% endblock %}